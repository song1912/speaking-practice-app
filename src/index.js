
import React from "react";
import ReactDOM from "react-dom/client";
import SpeakingPracticeApp from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <SpeakingPracticeApp />
  </React.StrictMode>
);
